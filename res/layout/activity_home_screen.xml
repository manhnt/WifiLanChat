<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".HomeScreenActivity" >

    <Button
        android:id="@+id/view_chat_rooms_btn"
        android:text="@string/view_chat_rooms"
        android:layout_width="180dp"
        android:layout_height="80dp"
        android:layout_centerHorizontal="true"
        android:textStyle="bold"
        android:textSize="24sp"
        android:layout_above="@+id/view_people_btn"
        android:layout_marginBottom="10dp"/>
    
    <Button 
        android:id="@id/view_people_btn"
        android:text="@string/view_member"
        android:layout_width="180dp"
        android:layout_height="80dp"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:textStyle="bold"
        android:textSize="24sp"/>
    
    <Button 
        android:id="@+id/settings_btn"
        android:text="@string/settings"
        android:layout_width="180dp"
        android:layout_height="80dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/view_people_btn"
        android:layout_marginTop="10dp"
        android:textStyle="bold"
        android:textSize="24sp"/>

</RelativeLayout>
